node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage
/playwright-report
/test-results
/dist
/package

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Editor
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code local settings
.claude/settings.local.json

# Local backend volumes
/.data/

# Visual brainstorming companion
.superpowers/

# Local Compose override (dev-only CL/MCP wiring — see HANDOFF; never committed)
/docker-compose.override.yml
# Local MCP server declaration (dev-only; mounted into the gateway via the override)
/mcp.yaml
# gen:api intermediate sanitized specs
/.tmp/
